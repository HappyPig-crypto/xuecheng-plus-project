spring:
  application:
    name: media-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev01
        group: xuecheng-plus-project
      config:
        namespace: dev01
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
#profiles默认为dev
  profiles:
    active: dev



